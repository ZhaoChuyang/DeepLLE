{
    "name": "base_exp",
    "resume_checkpoint": "",
    "data_factory": {
        "train": {
            "name": "TrainDataFactory",
            "args": {
                "arg1": "arg1"
            }
        },
        "valid": {
            "name": "TrainDataFactory",
            "args": {
                "arg1": "arg1"
            }
        }
    },
    "trainer": {
        "epochs": 1,
        "iters_per_epoch": -1,
        "early_stop": 10,
        "saved_period": 100,
        "save_dir": "event_logs",
        "verbosity": 2,
        "monitor": "min valid_loss"
   }
}