{
    "name": "base_exp",
    "resume_checkpoint": "",
    "trainer": {
        "epochs": 1,
        "early_stop": 10,
        "saved_period": 100,
        "save_dir": "events_logs",
        "verbosity": 2,
        "monitor": "min val_loss"
   }
}